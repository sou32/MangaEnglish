<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MangaEnglish.Pages.ChapterSelectPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:MangaEnglish.Models"
    BackgroundColor="#F7F7FA"
    Title="チャプターを選ぶ">

    <Grid Padding="20" RowDefinitions="Auto,Auto,*" RowSpacing="16">

        <Label
            x:Name="CurrentChapterLabel"
            Grid.Row="0"
            Text="現在のチャプター：未選択"
            FontSize="18"
            TextColor="#555"
            Margin="0,0,0,4" />

        <Label
            Grid.Row="1"
            Text="チャプターを選ぼう"
            FontSize="22"
            FontAttributes="Bold"
            TextColor="#333" />

        <CollectionView
            Grid.Row="2"
            x:Name="ChapterCollection"
            SelectionMode="Single"
            SelectionChanged="ChapterTapped">

            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="8" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Chapter">
                    <Border
                        BackgroundColor="White"
                        Stroke="#DDD"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 16"
                        Padding="18">
                        <Label
                            Text="{Binding Name}"
                            FontSize="20"
                            TextColor="#333" />
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

    </Grid>
</ContentPage>